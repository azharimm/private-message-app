import api from '../api/all';

const state = {
	conversation: null,
	loadingConversation: false
}

const getters = {
	//
}

const actions = {
	//
}

const mutations = {
	//
}

export default {
	state,
	getters,
	actions,
	mutations
}